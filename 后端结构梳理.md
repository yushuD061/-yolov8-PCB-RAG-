# 后端结构梳理

> PCB 缺陷检测系统后端 — FastAPI + Ultralytics YOLO + WebSocket  
> 模型：6 类 PCB 缺陷（missing_hole, mouse_bite, open_circuit, short, spur, spurious_copper）

---

## 1. 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| Web 框架 | FastAPI (Python 3.11+) | 异步支持，自动 OpenAPI 文档 |
| 推理引擎 | Ultralytics YOLO | `model.track()` 检测+追踪 / `model()` 单帧推理 |
| 通信 | WebSocket + REST API | WS 实时推送 + multipart 文件上传 |
| 视频处理 | OpenCV | 帧读取、JPEG 编码、VideoWriter 输出 |
| 配置 | JSON 文件 | `backend/data/config.json`，`ConfigManager` 类管理 |
| 存储 | SQLite | 告警记录、历史统计 |
| 数据模型 | Pydantic v2 | 严格类型校验，自动 JSON 序列化 |

---

## 2. 目录结构

```
backend/
├── main.py                  # 入口：FastAPI app + WS路由 + REST 端点 + 静态文件服务（PCB 版）
├── config.py                # ConfigManager：JSON 加载/保存/热更新/监听器
├── models.py                # Pydantic 模型（PCB 缺陷检测字段）
├── processor.py             # VideoProcessor：离线视频处理（PCB 版，无轨迹线）
├── core/
│   ├── yolo_engine.py       # YoloEngine：封装 YOLO 加载/检测/追踪/绘制
│   ├── pipeline.py          # Pipeline：后台主循环（PCB 版，6 类缺陷映射）
│   ├── video_source.py      # VideoSource：视频源管理（文件/RTSP/图片 + 环形缓冲）
│   └── hardware.py          # HardwareCollector：FPS/CPU/内存/温度采集
├── store/
│   ├── alarm_store.py       # 告警 SQLite 存储（简化 PCB 版）
│   └── history_store.py     # 日检测统计 SQLite 存储
└── data/
    ├── config.json          # 运行时配置（PCB 字段）
    └── uploads/             # 上传文件存储目录
```

---

## 3. 核心模块说明

### 3.1 `main.py` — 应用入口

**REST 端点（7 个）**：

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/detect/image` | POST | 图片检测：接收 multipart（`file`, `confidence`, `iou`）→ YOLO 推理 → 返回缺陷列表 + debug 信息 |
| `/api/upload` | POST | 视频上传：保存文件 → 启动 `VideoProcessor` 后台线程 → 返回 `task_id` |
| `/api/process/{task_id}` | GET | 进度轮询：返回 `{ percent, done, url, error }` |
| `/api/gemini/analyze` | POST | AI 分析（可选）：接收 base64 图片 + prompt → 返回预设报告 |
| `/api/health` | GET | 健康检查：`{ status: "healthy", timestamp }` |
| `/api/ping` | GET | 前后端通信测试 |
| `/debug` | GET | 管线状态调试 |

**WebSocket 路由**：
- 路径：`/ws`
- 连接即自动订阅 `telemetry` + `video_frame` 频道
- 推送拆分为两条独立消息：`targets_stream`（缺陷数据）+ `telemetry_metrics`（硬件遥测）
- 接收 `CONF_UPDATE` 消息

**静态文件服务**：
- `/video_output/` — 处理后的视频文件
- `/` — web2 构建产物（`web2/dist/`）

### 3.2 `core/yolo_engine.py` — 推理引擎

封装 Ultralytics YOLO，对外提供两个接口：

```python
class YoloEngine:
    def load(self, model_path: str)       # 加载 .pt 模型
    def track(self, frame, conf, iou)     # 单帧检测+追踪，返回 list[TrackedObject]
    def plot(self, frame)                 # 返回标注后的帧（检测框已绘制）
```

`TrackedObject` 数据类包含：`id, x, y, w, h, class_id, confidence`（无交通字段）。

坐标已转换为 0-100% 相对值，与前端 `dataAdapter.ts` 兼容。

### 3.3 `core/pipeline.py` — 实时管线

后台线程异步主循环：

```
while running:
    1. 取帧: video_source.read()
    2. 追踪: engine.track(frame, conf, iou) → list[TrackedObject]
    3. 构建推送数据: targets_data (id/type/className/x/y/width/height/confidence)
    4. 推送到队列: telemetry_queue.put_nowait({telemetry, targets})
    5. 视频帧推送: 每 200ms 一次，cv2.imencode JPEG → frame_queue
    6. 自适应睡眠: 目标 50ms 周期 (~20 FPS)
```

类别映射为 6 类 PCB 缺陷：`missing_hole`, `mouse_bite`, `open_circuit`, `short`, `spur`, `spurious_copper`。

### 3.4 `processor.py` — 离线视频处理器

独立于实时管线，专用于"先推理后播放"的视频处理：

```
VideoProcessor.start(video_path, config, progress_callback, done_callback)
  → 后台线程:
    1. 加载 YOLO 模型
    2. 逐帧读取视频
    3. model.track(frame, persist=True) 检测+追踪
    4. 绘制检测框 (results[0].plot())
    5. 写入输出视频 (cv2.VideoWriter)
    6. 每 5% 调用 progress_callback
    7. 完成调用 done_callback(success, error, output_path)
```

### 3.5 `core/video_source.py` — 视频源

支持三种输入模式：RTSP 流 / 文件路径 / 静态图片。环形缓冲 `RingBuffer` 实现线程安全读写。

### 3.6 `core/hardware.py` — 遥测

使用 `psutil` 采集 CPU/内存，`time.time()` 统计 FPS。NPU 和温度在 PC 环境返回 0。

---

## 4. 数据流

```
┌─────────────────────────────────────────────────────────────┐
│ 图片检测:                                                    │
│   前端 POST /api/detect/image                                │
│     → cv2.imdecode                                            │
│     → YOLO(model_path).predict(img)                           │
│     → 返回 { detections: [{class_id, className, confidence,   │
│          x1,y1,x2,y2, width,height}], debug: {...} }          │
│                                                              │
│ 视频检测:                                                    │
│   前端 POST /api/upload                                      │
│     → 保存到 data/uploads/                                   │
│     → processor.start(video_path, config, callbacks)         │
│     → 后台线程逐帧处理 → video_output/xxx_processed.mp4      │
│     → 前端轮询 GET /api/process/{task_id}                    │
│     → 完成 → 播放 /video_output/xxx_processed.mp4            │
│                                                              │
│ 实时检测 (RV1126B 板端):                                      │
│   前端 LiveDetectTab 连接 RTSP → WS订阅 targets_stream        │
│   pipeline 后台线程 → telemetry_queue → WS handler            │
│     → 拆分推送: targets_stream + telemetry_metrics           │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. WS 消息协议

### 5.1 推送（后端 → 前端）

```json
// 缺陷数据（PCB 版，无 speed/distance/path）
{ "type": "targets_stream", "data": [
    { "id": 1, "type": "defect", "className": "missing_hole",
      "x": 45.2, "y": 62.1, "width": 8.3, "height": 6.7,
      "confidence": 0.95 }
]}

// 遥测
{ "type": "telemetry_metrics", "data": {
    "fps": 15, "npu": 0, "cpu": 35, "memUsed": 1280,
    "memTotal": 4096, "temp": 45, "latency": 32, "selectedModel": "pcb_model"
}}

// 连接状态
{ "type": "status_change", "data": { "connected": true } }

// 日志
{ "type": "log_broadcast", "data": { "message": "[INFO] 配置已更新" } }
```

### 5.2 接收（前端 → 后端）

```json
{ "method": "CONF_UPDATE", "channel": "control_channel",
  "data": { "selectedModel": "pcb_model", "saveResults": true,
            "maxFps": 30, "npuMode": "balanced" } }
```

---

## 6. 配置文件

`backend/data/config.json`（PCB 版）：

```json
{
  "detectionConfidence": 0.45,
  "iouThreshold": 0.50,
  "selectedModel": "pcb_model",
  "saveResults": true,
  "retentionDays": 30,
  "jpegQuality": 85,
  "maxFps": 30,
  "npuMode": "balanced",
  "tempWarning": 75,
  "reviewThreshold": 0.35,
  "consecutiveAlerts": 5,
  "enableInferenceTelemetry": false
}
```

`ConfigManager` 支持：`load()` / `get()` / `update(partial)` / `on_change(callback)`。

---

## 7. 检测数据格式

### 7.1 图片检测响应

```json
{
  "detections": [
    {
      "class_id": 0,
      "className": "missing_hole",
      "type": "defect",
      "confidence": 0.95,
      "x1": 20.5, "y1": 35.2,
      "x2": 42.7, "y2": 53.8,
      "width": 22.2, "height": 18.6
    }
  ],
  "debug": {
    "model": "pcb_defect",
    "conf": 0.25,
    "iou": 0.45,
    "classes": { "0": "missing_hole", "1": "mouse_bite", ... }
  }
}
```

### 7.2 6 类缺陷

| class_id | 名称 | 中文 | 说明 |
|----------|------|------|------|
| 0 | missing_hole | 漏孔 | 焊盘或过孔缺失 |
| 1 | mouse_bite | 鼠咬 | 板边缺口/锯齿 |
| 2 | open_circuit | 开路 | 线路断裂 |
| 3 | short | 短路 | 线路意外连接 |
| 4 | spur | 毛刺 | 铜箔凸起 |
| 5 | spurious_copper | 残铜 | 不应存在的铜箔 |

---

## 8. 后端改造记录（已完成 ✅）

后端已从原交通检测代码全面改造为 PCB 缺陷检测，以下为改造内容：

| # | 任务 | 文件 | 说明 |
|---|------|------|------|
| 1 | 更新类别映射 | `core/pipeline.py` | `_CLASS_NAMES` 替换为 6 类 PCB 缺陷：`missing_hole`, `mouse_bite`, `open_circuit`, `short`, `spur`, `spurious_copper` |
| 2 | 移除 estimator | `core/estimator.py` | 已删除（PCB 不需要速度/车距估算） |
| 3 | 简化管线 | `core/pipeline.py` | 移除 estimator 调用、轨迹线、车道逻辑 |
| 4 | 简化硬件遥测 | `core/hardware.py` | 保留基础 FPS/CPU/内存采集 |
| 5 | 更新数据模型 | `models.py` | `DetectionTarget` 移除 `speed`/`distance`/`licensePlate`/`pedestrianStatus`/`warning`/`path`，新增 `confidence`/`defectType`；`SystemConfig` 重新设计 |
| 6 | 更新配置 | `data/config.json` | 移除交通字段，新增 PCB 配置 |
| 7 | 适配 WS 消息 | `main.py` | `_FRONTEND_TO_BACKEND` 精简；推送/seek/image_detect 全链路 PCB 格式 |
| 8 | 简化存储 | `store/alarm_store.py` `store/history_store.py` | 告警表简化，日统计精简 |

> **注意**：完整运行需要将 PCB 缺陷检测 `.pt` 模型文件放入 `model/pcb_model.pt`（配置已默认指向此路径）。
