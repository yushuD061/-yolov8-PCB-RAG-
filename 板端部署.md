# YOLOv8 模型部署到 RV1126B — 板端部署指南

> 将训练好的 YOLOv8 RKNN 模型部署到 RV1126B NPU 上运行实时 PCB 缺陷检测，并将画面（含检测框）推送到 PC 前端

---

## 1. 部署架构

### 方案 D — fb_ws_stream ✅ 推荐（无需 mediamtx）

```
RV1126B 板端
┌────────────────────────────────────┐
│  rknn_yolov8_cam (官方程序)        │
│  → 摄像头采集 + NPU 推理           │
│  → 画框显示在 /dev/fb0 (framebuffer)│
└────────────┬───────────────────────┘
             │ 已有检测框
             ▼
┌────────────────────────────────────┐
│  fb_ws_stream.py (Python)          │
│  → 调用 ffmpeg 截取 /dev/fb0        │
│  → ffmpeg: fbdev → MJPEG → stdout  │
│  → 读取 JPEG 帧 → WebSocket 推流    │
└────────────┬───────────────────────┘
             │ WebSocket (binary JPEG)
             ▼
PC 端（Python 后端 + 前端）
· backend/main.py → 接收 binary → frame_queue → 转发前端
· web2 → <img> 显示 + DOM 缺陷框叠加
```

**优点**：不需要 mediamtx，不需 RTSP，板端只需 ffmpeg + Python websockets。视频帧自带检测框（因为截的是板端屏幕）。

### 方案 C — 截屏 RTSP（需 mediamtx）

```
RV1126B 板端
┌─────────────────────────────────────┐
│  rknn_yolov8_cam (官方程序)         │
│  → 摄像头采集 + NPU 推理            │
│  → 画框显示在板端屏幕 (framebuffer)  │
└────────────┬────────────────────────┘
             │ 截屏
             ▼
┌─────────────────────────────────────┐
│  ffmpeg                              │
│  → 从 /dev/fb0 读取帧缓存            │
│  → libx264 编码                     │
│  → 推送 RTSP 流                     │
└────────────┬────────────────────────┘
             │ RTSP
             ▼
┌─────────────────────────────────────┐
│  mediamtx (RTSP 服务器)             │
│  → 接收 ffmpeg 推流                 │
│  → 提供 RTSP:// 地址供消费          │
└────────────┬────────────────────────┘
             │ 网络
             ▼
PC 后端: ffmpeg RTSP→HLS → 前端 hls.js 播放
```

---

## 2. 环境速览

| 组件 | 路径/版本 |
|------|-----------|
| 板端系统 | ATK-DLRV1126B (Buildroot) |
| RKNPU 驱动 | 0.9.8 |
| RKNN Lite2 | 2.3.2 |
| OpenCV | 4.8.1 |
| numpy | 2.4.6 ⚠️ |
| CPU | Cortex-A53 |

---

## 3. 四种方案对比

| 方案 | 文件 | 说明 | 现状 |
|------|------|------|------|
| **D — fb_ws_stream ✅** | `官方程序 + fb_ws_stream.py` | 自动启动官方程序+解析 stdout 检测结果；kmsgrab 抓屏→MJPEG→WS 推流 | ✅ **当前方案** |

---

## 4. 方案 D：fb_ws_stream 直推 ✅ 推荐

> 无需 mediamtx，无需 RTSP。板端 ffmpeg 截屏 → MJPEG → Python WebSocket 直推到 PC 后端。

### 5.1 启动顺序（板端）

```bash
# 1. 官方模型后台运行（在 framebuffer 上画检测框）
nohup ./rknn_yolov8_cam model/yolov8_best.rknn 31 > /dev/null 2>&1 &

# 2. 启动 fb_ws_stream 推流（替换 PC_IP 为实际 IP）
python3 fb_ws_stream.py --host 192.168.50.1 --port 5000 --fps 5
```

### 4.2 参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--host` | `192.168.50.1` | PC 后端 IP |
| `--port` | `5000` | PC 后端端口 |
| `--fps` | `5` | 截取帧率（1-25） |
| `--quality` | `6` | JPEG 质量 2-31（越小越好，建议 4-8） |
| `--fb` | `/dev/fb0` | framebuffer 设备路径 |

### 4.3 PC 端

```bash
# 后端
cd backend && python main.py

# 前端（另开终端）
cd web2 && npm run dev
```

### 4.4 前端效果

1. 打开 `http://localhost:5173` → RV1126B 实时检测 Tab
2. 板端连接后，前端自动显示实时画面（视频帧自带检测框，无需额外 RTSP 配置）
3. 右侧面板显示板端状态（FPS 等遥测数据）

> **原理**：fb_ws_stream 截取的是板端 `/dev/fb0` 屏幕——官方推理程序已经把检测框画在上面了，所以推送到前端的每一帧天然带框。


## 5. 方案 C：截屏 RTSP + HLS（需 mediamtx）

### 5.1 启动顺序（板端）

```bash
# 1. 官方模型后台运行
nohup ./rknn_yolov8_cam model/yolov8_best.rknn 31 > /dev/null 2>&1 &

# 2. RTSP 服务器后台运行
mediamtx > /dev/null 2>&1 &

# 3. ffmpeg 截屏推流（前台运行，Ctrl+C 停止）
ffmpeg -f fbdev -i /dev/fb0 \
  -c:v libx264 -preset ultrafast -tune zerolatency \
  -f rtsp rtsp://localhost:8554/live
```

### 5.2 PC 端

```bash
# 后端
cd backend && python main.py

# 前端（另开终端）
cd web2 && npm run dev
```

### 5.3 前端操作

1. 打开 `http://localhost:5173` → RV1126B 实时检测 Tab
2. RTSP 地址填 `rtsp://192.168.50.119:8554/live`
3. 点「连接 RTSP」按钮，等待画面显示

> 画面自带检测框，因为截的是板端屏幕输出，官方程序已经在上面画好了。

---

## 6. 方案 A 详述：Python 直推（遇到的问题）

### 6.1 后处理

YOLOv8 RKNN 输出 9 个 tensor（3 个尺度 × 3 路子输出）：

| 子索引 | 名称 | Shape | 含义 |
|--------|------|-------|------|
| `i*3+0` | conv2d | `(1, 64, H, W)` | bbox 回归 (4×reg_max=64) |
| `i*3+1` | sigmoid | `(1, 6, H, W)` | 类别置信度（已 sigmoid） |
| `i*3+2` | clamp | `(1, 1, H, W)` | 分数门控（已 sigmoid） |

**关键规则**（对齐官方 C++ 代码 `postprocess.cc`）：

1. **不要双重 sigmoid** — cls 和 score_sum 的输出已经是 sigmoid 后的值
2. **score_sum 是快速过滤门** — `if score_sum < threshold: skip`，不是和 cls 相乘
3. **置信度 = max(cls)** — 直接取 6 类中最大值，不乘以 score_sum
4. **NMS 按类别独立运行** — 同类框之间做 NMS，不是全局 NMS

### 6.2 预处理

官方 C++ 程序的预处理：

```python
def preprocess(frame):
    img = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)
    img = cv2.rotate(img, cv2.ROTATE_90_CLOCKWISE)  # 摄像头横装
    # letterbox：保持宽高比，灰边填充
    scale = min(640 / w, 640 / h)
    resized = cv2.resize(img, (nw, nh))
    letterbox = np.full((640, 640, 3), 114, dtype=np.uint8)
    letterbox[pad_y:pad_y+nh, pad_x:pad_x+nw] = resized
    # /255.0 归一化（RKNN 输入 zp=-128, scale=0.003922）
    return np.ascontiguousarray(np.expand_dims(letterbox/255.0, 0), dtype=np.float32)
```

**注意导入顺序**：板端 numpy 2.4.6 与 OpenCV 4.8.1 不兼容，必须 `import cv2` 在 `import numpy` 之前，否则 OpenCV 函数报错"img is not a numpy array"。

### 6.3 letterbox 坐标反算

模型输出坐标在 640×640 模型空间，需要反算回原始图像（640×480，旋转后）：

```python
boxes[:, 0] = (boxes[:, 0] - pad_x) / scale   # x
boxes[:, 1] = (boxes[:, 1] - pad_y) / scale   # y
```

---

## 7. 已知问题

### 7.1 numpy 2.x + OpenCV 4.8.1 不兼容

- **症状**：`cv2.imencode('.jpg', img)` 报错 "img is not a numpy array"
- **原因**：numpy 2.x 改动了 C API，OpenCV 4.8.1 不兼容
- **方案 A**：`import cv2` 必须在 `import numpy` 之前（numpy 会自动加载兼容层）
- **方案 B**：用 `PIL.Image` 替代 OpenCV 的 JPEG 编码
- **方案 C**：用 `ffmpeg` 命令行替代 Python 编码

### 7.2 模型量化后 cls 头被压死

- **症状**：score_sum 和 cls 输出全部为常数（如 0.000019、0.001961），无法检出
- **原因**：RKNN int8 量化的校准集不具代表性，分类头 scale factor 过大，所有权重归零
- **解决**：用 50-200 张实际 PCB 图片作为校准集重新 `rknn.build()`
- **验证**：运行官方 `./rknn_yolov8_cam model/yolov8_best.rknn 31` 看板端屏幕能否检出

### 7.3 queue.Full 导致 asyncio 事件循环卡死

- **症状**：板端连接后几秒内断开，后端日志无异常
- **原因**：`queue.Queue(maxsize=8)` 被板端数据快速填满，`put()` 阻塞整个事件循环
- **解决**：后端用 `put_nowait()` 并丢弃旧数据（`_put_queue` 函数）；队列容量增大到 32

### 7.4 摄像头无法同时被两个进程打开

- RV1126B 的 MIPI CSI 摄像头（`/dev/video-camera0` → video31）同一时间只能被一个进程占用
- 官方程序 `rknn_yolov8_cam` 和 Python OpenCV 不能同时打开同一个摄像头
- 解决方案：用截屏方式（读 `/dev/fb0`）而非独立抓帧

### 7.5 前端画面卡顿

- 取决于 JPEG 质量和帧率
- 建议：`IMWRITE_JPEG_QUALITY=40`, `FRAME_SKIP=5`（每 5 帧发 1 帧）

---

## 8. 文件清单

```
板端 /userdata/aidemo/atk_rknn_yolov8_cam/
├── model/
│   ├── yolov8_best.rknn           # RKNN 模型文件
│   └── my_labels.txt              # 6 类标签
├── rknn_yolov8_cam                # 官方推理可执行文件
├── lib/                           # 动态库
├── fb_ws_stream.py                # ✅ 当前推流脚本

PC 项目根目录 D:\agent\test4\
├── backend/main.py                # FastAPI 后端（WS 转发）
├── web2/                          # Vite 前端
│   └── src/components/LiveDetectTab.tsx
├── scripts/
│   ├── fb_ws_stream.py            # 板端推流脚本
│   └── camera_stream.py           # NPU 直推脚本（参考）
└── 板端部署.md                    # 本文档
```

---

## 9. 标签文件

`my_labels.txt`（与模型训练时类别顺序一致）：

```
missing_hole
mouse_bite
open_circuit
short
spur
spurious_copper
```

---

## 10. 参考资源

| 资源 | 链接 |
|------|------|
| RKNN Toolkit 2 | https://github.com/airockchip/rknn-toolkit2 |
| 正点原子 RV1126B SDK | ATK-DLRV1126B-SDK |
| MediaMTX | https://github.com/bluenviron/mediamtx |
