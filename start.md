# PCB 缺陷检测系统 — 启动指南

## 项目概述

原交通检测系统改造为 PCB 缺陷检测系统，支持 **6 类 PCB 缺陷**识别：

| 缺陷 | 中文 | 颜色 |
|------|------|------|
| missing_hole | 漏孔 | 🔴 `#ff453a` |
| mouse_bite | 鼠咬 | 🟠 `#ff9f0a` |
| open_circuit | 开路 | 🟣 `#bf5af2` |
| short | 短路 | 🔵 `#0a84ff` |
| spur | 毛刺 | 🟢 `#30d158` |
| spurious_copper | 残铜 | 🟡 `#ffd60a` |

三种检测模式：
- **RV1126B 实时检测** — RTSP 板端视频流 → 实时推理 → 缺陷框叠加
- **图片检测** — 上传 PCB 图片 → YOLO 推理 → 缺陷标注
- **视频检测** — 上传 PCB 视频 → 离线处理 → 播放结果

---

## 环境要求

| 工具 | 版本 | 用途 |
|------|------|------|
| Python | >= 3.11 | 后端推理服务 |
| Node.js | >= 20 | 前端开发 |
| npm | >= 9 | 前端依赖管理 |

Python 依赖：

```bash
pip install ultralytics fastapi uvicorn opencv-python python-multipart psutil
```

> 首次运行前请确保已安装所有依赖，并执行 `cd web2 && npm install` 安装前端依赖。

---

## 快速启动

### 一键启动（推荐）

项目根目录执行：

```bash
python start.py
```

自动同时启动后端 (FastAPI :5000) 和前端开发服务器 (Vite :5173)，按 `Ctrl+C` 停止所有服务。

### 分别启动

#### 1. 启动后端（终端 1）

```bash
cd backend
python main.py
# → http://localhost:5000
# → WebSocket ws://localhost:5000/ws
```

> ✅ 后端已改造为 PCB 缺陷检测。详见 `后端结构梳理.md`。

### 2. 启动前端（终端 2）

```bash
cd web2
npm install    # 首次运行
npm run dev
# → http://localhost:5173
```

Vite 自动代理 `/ws` 和 `/api` 到后端 `localhost:5000`。

### 3. 登录

| 账户 | 密码 | 角色 |
|------|------|------|
| `admin` | `pcb_admin` | 管理员 |
| `guest` | `guest_pass` | 访客 |

---

## 页面导航

登录后侧边栏显示以下 Tab（按推荐使用顺序）：

| Tab | 功能 | 说明 |
|-----|------|------|
| 🖥️ RV1126B 实时检测 | RTSP 板端接入 + 实时缺陷框 | 配置 RTSP 地址 → 连接板端 → 自动推理 |
| 🖼️ PCB 图片检测 | 单张 PCB 图片上传检测 | 拖拽/选择图片 → 开始检测 → 6 色缺陷框 |
| ▶️ 视频检测 | PCB 视频离线处理 | 上传视频 → 后端逐帧推理 → 播放结果 |
| 📋 检测历史 | 缺陷检测记录 | 搜索/查看详情/清空 |
| ⚙️ 系统配置 | 全局设置 | 模型/存储/板端参数/告警阈值/系统信息 |
| 👤 个人中心 | 账户与日志 | 修改名称/退出登录 |

---

## 系统配置说明

全局配置包含以下板块，各 Tab 中的置信度 / IoU 为独立调节（不在此重复）：

| 板块 | 配置项 |
|------|--------|
| 模型配置 | 推理模型选择 |
| 检测存储 | 自动保存开关 / 保留天数 / 图像质量 |
| RV1126B 板端参数 | 最大帧率 / NPU 频率模式 / 温度告警阈值 |
| 告警与通知 | 复查置信度阈值 / 连续缺陷告警数 |
| 系统信息 | 版本 / 引擎 / 缺陷类型 + 导出配置 |

---

## 后端配置

`backend/data/config.json` — `selectedModel` 设为 PCB 模型文件名（如 `pcb_defect`，对应 `model/pcb_defect.pt`）。

---

## 项目状态

| 模块 | 状态 | 说明 |
|------|------|------|
| 前端 | ✅ 已完成 | 所有 Tab + LiveDetectTab + ConfigTab 重构 |
| 后端 | ✅ 已完成 | 8 项改造全部完成，适配 PCB 缺陷检测 |
| 模型 | ⏳ 待放置 | 配置已指向 `pcb_model.pt`，需用户提供模型文件放入 `model/` 目录 |

详见 `progress.md` 完整可执行计划。

---

## 文档

| 文件 | 说明 |
|------|------|
| `progress.md` | 可执行改造计划 |
| `前端结构梳理.md` | 前端架构说明 |
| `后端结构梳理.md` | 后端架构说明（含待改造项） |
| `修改记录.md` | 变更日志 |
| `板端部署.md` | YOLO26n 模型部署到 RV1126B 完整指南 |
