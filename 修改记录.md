# 修改记录

## 2026-07-18 11:17:38 +08:00 RAGAS 第 7 步低分与裁判稳定性分析

| 文件 | 修改内容 | 原因 | 验证方式 | 修改者 |
|---|---|---|---|---|
| `rag_eveal/analyze_low_scores.py` | 新增低分筛选、5 类代表样本选择、3 次重复裁判、均值/极差/标准差计算、离线检索对照和初步归因 | 执行第 7 步并形成可重复运行的分析入口 | 完成 60/60 个指标任务；输出 15 行重复记录 | Codex |
| `rag_eveal/results/judge_stability_20260718_111236.csv` | 保存 5 条代表样本各 3 次的四项裁判分数及聚合统计 | 量化裁判波动，避免凭单次分数设门禁 | 5 个 Sample ID、repeat=1/2/3；四项指标均 15/15 有效 | Codex |
| `rag_eveal/results/ragas_low_scores_20260718_111236.md` | 保存 32 条低分/缺失样本的问题、召回 ID、离线对照、回答、参考答案、初步原因和修复顺序 | 区分检索、精排、回答、参考答案与裁判问题 | 核对 Faithfulness 24+2 缺失、Answer Relevancy 12+1、Context Recall 22+1；去重后 32 条 | Codex |
| `rag_eveal/RAGAS_EVALUATION_PROGRESS.md`、`progress.md` | 将第 7 步更新为完成，总体进度约 74%，第 8 步设为下一步 | 保持专项和根进度一致 | 对照稳定性 CSV 与低分报告逐项复核 | Codex |

结论：5 条代表样本中 4 条至少一个指标满足极差 >= 0.3 或标准差 >= 0.15；Factual Correctness 与其他指标存在稳定冲突，继续只作诊断，不设硬门禁。

## 2026-07-18 10:54:01 +08:00 RAGAS 第 6 步完整结果回填

| 文件 | 修改内容 | 原因 | 验证方式 | 修改者 |
|---|---|---|---|---|
| `rag_eveal/RAGAS_EVALUATION_PROGRESS.md`、`progress.md` | 将第 6 步更新为完成，记录 43 条全量结果、3025.92 秒耗时、六项指标、有效计数及门槛结论，并将第 7 步设为当前下一步 | 正式结果已于 2026-07-17 19:42:13 落盘，原进度仍停留在待执行状态 | 解析逐条 CSV 为 43 行；对照摘要 JSON 的时间、配置、均值、有效数和阈值逐项复核 | Codex |

第 6 步三个质量门槛均未通过；缺失裁判值未按 0 分计入均值，Factual Correctness 继续仅作诊断指标。

## 2026-07-17 18:42:55 +08:00 RAGAS 第 4 步全量离线 ID 检索评估

| 文件 | 修改内容 | 原因 | 验证方式 | 修改者 |
|---|---|---|---|---|
| `rag_eveal/id_retrieval_metrics.py` | 新增 43 条全量离线检索入口；固定 Top-3、本地词级+字符级 TF-IDF，禁用生成、改写、LLM reranker 和外部 Embedding；输出 Precision/Recall、Hit@1/3、MRR、所有未命中及 SHA/Git 配置元数据 | 执行 `RAGAS_EVALUATION_PROGRESS.md` 第 4 步并形成可复现正式结果 | `python rag_eveal/id_retrieval_metrics.py` 成功处理 43/43 条；所有建议阈值通过 | Codex |
| `rag_eveal/results/id_metrics_pcb_nasa_20260717_184133.json` | 新增不可变逐样本结果；Hit@1=0.8837、Hit@3=0.8837、MRR=0.8837、ID Recall@3=0.8837、5 条 Top-3 未命中 | 保存正式评估证据，避免只保留终端平均分 | PowerShell JSON 解析成功；样本 43 条、未命中 5 条、数据集 SHA-256 已记录 | Codex |
| `rag_eveal/RAGAS_EVALUATION_PROGRESS.md`、`progress.md` | 将第 4 步更新为完成、总体进度更新为约 57%，并将第 6 步标记为下一步 | 保持专项和根项目进度一致 | 对照正式结果 JSON、阈值和输出文件逐项复核 | Codex |

验证补充：`python -m unittest discover -s rag_eveal/tests -p "test_*.py" -v` 通过 2/2；本次没有执行第 6 步，也没有调用外部模型。

## 2026-07-16 19:43:04 +08:00 RAGAS 评估进度文档化

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `rag_eveal/RAGAS_EVALUATION_PROGRESS.md` | 新增 RAGAS 十步计划状态、精确依赖、数据集与 Smoke Test 结果、读写文件矩阵、禁止覆盖项、后续步骤、阈值和风险 | 将当前评估工作形成可持续维护和可交接的单一事实来源 | 依据当前代码、CSV 行数、`uv.lock`、虚拟环境包版本和结果 CSV 逐项复核 | Codex |
| `progress.md` | 新增 2026-07-16 RAGAS 评估计划进度表，标明第 3 步完成、第 4 步待执行 | 保持根项目进度与专项文档一致 | 对照专项十步状态表和证据文件 | Codex |
| `修改记录.md` | 记录本次文档变更、原因和验证方式 | 满足项目变更可追溯要求 | Markdown 表格和时间/时区检查 | Codex |

> 请在提交前执行 `git status` 和 `git diff`，确认不提交 `.env.ragas`、`backend/agent/.env` 或任何真实 API Key。

## 2026-07-13 LangSmith 依赖安装

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `.venv`、`requirements.txt`、`pyproject.toml` | 安装并固定 `langsmith==0.10.2` | 为 RAG 链路引入可选可观测性 SDK | PyPI 实时解析、导入及版本检查 | Codex |
| `LANGSMITH.md` | 新增兼容版本、安装、配置、安全边界和后续接入说明 | 支持安全启用与项目交接 | 检查命令、环境变量和默认关闭说明 | Codex |
| `PROJECT_STRUCTURE.md`、`progress.md`、`修改记录.md` | 同步依赖审计、进度和变更记录 | 保持项目文档可追溯 | Markdown 内容复核 | Codex |

> 请在提交前执行 `git status` 和 `git diff`；不要提交任何真实 `LANGSMITH_API_KEY`。

## 2026-07-13 历史目标编号纠正

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/HistoryTab.tsx`、`web2/src/types.ts`、`web2/src/App.tsx` | 新记录保存 `className`；旧图片记录根据英文类别或中文描述解析 0–5 类别编号 | 修复旧顺序号导致开路显示为 1 的问题 | 读取两个模型类别表并通过 TypeScript 检查 | Codex |

## 2026-07-13 缺陷编号回退到 0–5

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/HistoryTab.tsx`、`web2/src/App.tsx` | 类别编号恢复模型原始 0–5，并反向迁移上一版改成 1–6 的图片检测历史 | 按用户要求回退编号规则 | TypeScript 检查及六类顺序对照 | Codex |

## 2026-07-13 缺陷编号调整为 1–6

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/HistoryTab.tsx`、`web2/src/App.tsx` | 界面类别编号由 0–5 调整为 1–6，并一次性迁移已有图片检测历史 | 符合用户指定的编号规则 | TypeScript 检查及六类顺序对照 | Codex |

## 2026-07-13 检测历史缺陷编号说明

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/HistoryTab.tsx` | 在记录详情底部新增 0–5 六类缺陷的中文、英文编号说明 | 帮助用户解释检测记录 ID | TypeScript 检查及类别顺序对照 | Codex |
| `web2/src/components/ImageRecTab.tsx`、`web2/src/App.tsx` | 图片历史 ID 改用真实 `class_id`，不再使用数组顺序号 | 保证记录 ID 与模型类别映射一致 | TypeScript 检查 | Codex |

## 2026-07-13 RAG 对话单条与批量删除

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/RagTab.tsx` | 每条对话增加删除按钮与多选框；新建按钮旁增加带数量的批量删除 | 支持精确和批量管理历史对话 | TypeScript 检查及当前/非当前/全部删除状态复核 | Codex |

## 2026-07-13 RAG 统一文档管理模块

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/RagTab.tsx` | 将文档上传、LLM 配置和文档列表重绘为一个统一模块，使用统一标题、数量和右收按钮 | 减少割裂的面板状态并统一文档操作 | TypeScript 检查及上传、删除、收起/展开复核 | Codex |

## 2026-07-13 RAG 独立对话栏与右收文档面板

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/RagTab.tsx` | 历史对话恢复为独立固定栏；文档上传和文档列表取消上下折叠，改为分别向右收起 | 匹配最新页面交互要求 | TypeScript 检查及两个面板四种组合状态复核 | Codex |

## 2026-07-13 RAG 左右停靠栏

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/RagTab.tsx` | 历史对话与主对话组合为左侧工作区；历史栏向左停靠折叠，文档栏向右停靠折叠，中间自动扩展 | 提升对话工作区利用率 | TypeScript 检查与展开/收起宽度状态复核 | Codex |

## 2026-07-13 RAG 三栏布局

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/RagTab.tsx` | 左侧改为历史对话管理，中间为主对话，右侧为可分别折叠的文档上传与文档列表 | 按操作流重新组织 RAG 页面 | TypeScript 检查与三栏滚动/折叠状态复核 | Codex |

## 2026-07-13 RAG 对话删除

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/components/RagTab.tsx` | 新增删除当前对话；删除后自动切换，最后一个会话删除后自动创建空白会话 | 完善 RAG 多会话管理 | TypeScript 检查及单会话/多会话状态复核 | Codex |

## 2026-07-13 历史滚动与 RAG 多会话

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `web2/src/App.tsx`、`web2/src/components/HistoryTab.tsx` | 补齐页面高度约束，记录列表和详情独立滚动 | 修复检测历史无法滚动 | TypeScript 检查和布局类复核 | Codex |
| `web2/src/components/RagTab.tsx` | 左侧文档、右侧对话独立滚动；增加对话持久化、切换和新建对话 | 防止长对话拉伸文档列表并支持历史会话 | TypeScript 检查与 localStorage 状态复核 | Codex |

## 2026-07-13 检测存储配置落地

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `backend/routes/detect.py` | 应用保存开关、JPEG 质量及图片保留期，保存路径为 `backend/data/detection_results/` | 让检测存储配置真正影响检测流程 | JPEG 编码质量、开关和过期文件清理测试 | Codex |
| `backend/store/inspection_store.py`、`backend/store/alarm_store.py` | 增加按保留天数清理数据库记录 | 让结果保留天数同时作用于历史数据 | 临时数据库边界时间测试 | Codex |
| `backend/main.py`、`web2/src/App.tsx` | 新增 REST 配置读写，前端配置不再依赖 WebSocket 在线状态 | 确保设置可靠持久化 | API 配置更新与前端类型检查 | Codex |
| `web2/src/components/ConfigTab.tsx` | 关闭保存后禁用保留期和图片质量控件 | 直观反映配置实际状态 | TypeScript 类型检查 | Codex |

## 2026-07-13 检测历史日历与批次筛选

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `backend/store/inspection_store.py` | 增加 `batch_id` 自动迁移、持久化及时间/批次查询条件 | 支持按生产批次和时间分析检测数据 | 验证旧表迁移、批次写入和组合筛选 | Codex |
| `web2/src/components/ImageRecTab.tsx`、`web2/src/App.tsx` | 每次批量检测生成统一批次编号并随历史上报 | 建立图片与检测批次的关联 | TypeScript 类型检查 | Codex |
| `web2/src/components/HistoryTab.tsx` | 新增开始/结束日期日历、批次下拉、重置筛选及批次展示 | 支持按时间范围和批次查看检测历史 | TypeScript 类型检查与边界日期复核 | Codex |

> 旧版浏览器历史没有批次字段，兼容显示为“历史记录（无批次）”。

## 2026-07-13 RAG 检测历史与良品率分析

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `backend/store/inspection_store.py` | 新增按 PCB 检测批次持久化及良品率、不良率、缺陷分布统计 | 缺陷告警条数无法作为良品率分母 | 使用良品、不良品、多缺陷样本验证公式 | Codex |
| `backend/routes/detect.py` | 新增检测批次上报和统计接口 | 让检测历史成为后端持久化数据源 | 验证零缺陷和多缺陷上报响应 | Codex |
| `backend/routes/rag.py` | RAG 自动注入检测总数、良品/不良品、良品率和缺陷分布 | 支持自然语言质量分析 | 模拟历史后验证上下文统计值 | Codex |
| `web2/src/components/ImageRecTab.tsx`、`web2/src/App.tsx` | 每张成功检测图片均上报，包含零缺陷良品 | 完整采集良品率分母 | TypeScript 类型检查 | Codex |

> 旧版记录只有缺陷告警，没有完整检测总数，不能可靠反推良品率；新统计从本次功能启用后的检测批次开始累计。

## 2026-07-13 RAG 文档删除一致性修复

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `backend/agent/platform/postgres.py` | PostgreSQL 删除返回实际行数，失败不再固定返回成功 | 修复 Docker 数据残留且无错误反馈的问题 | 单元模拟 SQL 成功与失败返回值 | Codex |
| `backend/agent/rag/rag_engine.py` | 先删除外部存储，成功后删除 SQLite，并返回各存储结果 | 避免前端列表先消失造成假成功 | 验证删除顺序及异常传播 | Codex |
| `backend/routes/rag.py` | DELETE API 返回删除详情或明确错误 | 提升跨存储删除可观测性 | 检查成功/失败响应结构 | Codex |
| `web2/src/components/RagTab.tsx` | 检查 HTTP 和业务响应，失败时保留列表并输出日志 | 避免静默吞掉删除错误 | TypeScript 类型检查 | Codex |

## 2026-07-13 RAG 文档入库时间修复

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `backend/agent/rag/rag_engine.py` | 将 SQLite 无时区 UTC 时间统一转换为末尾带 `Z` 的 ISO 8601 | 防止浏览器将 UTC 时间误判为本地时间 | 验证历史时间转换与带时区时间幂等处理 | Codex |
| `web2/src/components/RagTab.tsx` | 按浏览器本地时区格式化，并为无效时间提供降级显示 | 显示真实本地入库时间 | TypeScript 类型检查 | Codex |
| `progress.md`、`修改记录.md` | 同步进度与变更记录 | 保持项目状态可追溯 | 检查记录字段完整性 | Codex |

## 2026-07-13 RAG 文档模块迁移

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `backend/agent/document/` | 从 `example/` 迁移文档模型与解析器，新增包导出；PDF 主解析器适配项目已有 `pypdf` | 统一 RAG 文档领域模型和解析入口 | 执行模块导入、文本解析及 Python 编译检查 | Codex |
| `backend/routes/rag.py` | 上传接口改用统一 `parse_bytes`，返回解析器、页数、字符数和 OCR 提示 | 消除路由内重复解析逻辑并增强可观测性 | 使用 UTF-8、GBK、空文本和无效 PDF 用例验证 | Codex |
| `progress.md`、`修改记录.md` | 同步迁移进度与变更记录 | 保持项目状态可追溯 | 检查状态、证据与阻塞项字段 | Codex |

> `Document`/`DocumentVersion` 已迁移为领域模型，但当前数据库仍使用既有 `rag_docs`/`rag_chunks`；版本表持久化不在本次迁移范围。

## 2026-07-13 PostgreSQL Docker 打包

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `docker-compose.postgres.yml` | 新增 PostgreSQL 16 Alpine 服务、健康检查、本地端口绑定、持久化卷和独立网络 | 提供可重复启动的本地 PostgreSQL 容器 | 执行 `docker compose config`，并在 Docker 可用时检查健康状态 | Codex |
| `.env.postgres.example` | 新增无真实凭据的数据库配置模板 | 分离公开模板与本地密码 | 检查变量与后端 `PG_*` 配置映射 | Codex |
| `POSTGRES_DOCKER.md` | 新增启动、后端接入、验证、运维和备份说明 | 降低部署和交接成本 | 对照 Compose 服务名、端口、卷及后端配置 | Codex |
| `.gitignore` | 忽略本地环境文件、缓存、运行数据库、备份和构建产物 | 防止凭据与运行数据误提交 | 使用 `git check-ignore` 验证敏感文件规则 | Codex |
| `progress.md`、`修改记录.md` | 同步进度与变更记录 | 保持项目文档可追溯 | 检查字段与证据文件 | Codex |

> 提醒：请及时执行 `git status`、`git diff` 复核变更，确认后再提交并推送；本次未代为执行 Git 提交。

## 2026-07-13 09:15:35 +08:00

| 修改位置 | 变更摘要 | 原因 | 验证方式 | 操作者 |
|---|---|---|---|---|
| `PROJECT_STRUCTURE.md` | 新增项目概览、Mermaid 架构图、目录树、技术栈、Python 依赖审计、数据流、部署及风险说明 | 按 `project-structure-overview` skill 对当前项目进行可复核盘点 | 对照入口、路由、配置、依赖声明和锁文件；检查 Mermaid 节点/连线与 Markdown 表格 | Codex |
| `progress.md` | 新增符合统一字段要求的项目结构盘点进度表 | 保留现有执行计划并补充实时盘点状态 | 检查负责人、状态、完成度、证据、阻塞项和更新时间列 | Codex |
| `修改记录.md` | 记录本次文档整理的时间、位置、原因、验证方式和操作者 | 保持项目变更可追溯 | 检查本条记录字段完整性 | Codex |

> 提醒：请及时执行 `git status`、`git diff` 复核变更，确认后再提交并推送；本次未代为执行 Git 提交。

## 2026-06-21

### Query Rewriter 迁移 — history-aware multi-query 改写

| 文件 | 改动 |
|------|------|
| `backend/agent/rewriter.py` → `backend/agent/rag/rewriter.py` | **新建→移至 rag/** — 从 `example/reweiter.py` 迁移：`LLMRewriter` 类、`HistoryMessage` 数据类、辅助函数；后移至 `rag/` 子包内统一管理 |
| `backend/agent/rag/__init__.py` | import 路径 `agent.rewriter` → `agent.rag.rewriter` |
| `backend/agent/rag/rag_engine.py` | import 路径 `agent.rewriter` → `agent.rag.rewriter` |
| `backend/agent/config.py` | `AgentConfig` 新增 `rag_rewrite_queries: int = 3`（0 或 1 表示禁用）；`load_agent_config` 新增 `RAG_REWRITE_QUERIES` 环境变量映射 |
| `backend/agent/rag/rag_engine.py` | 新增 import `LLMRewriter`/`HistoryMessage`；`RagEngine` 新增 `_rewriter` 属性、`set_rewriter()` 方法（读取配置控制启用/禁用）；`query()` 新增 `history` 参数，rewriter 启用时执行多路检索合并；新增 `_multi_query_search()` 方法 |
| `backend/agent/rag/__init__.py` | 导出 `LLMRewriter`、`HistoryMessage` |
| `backend/routes/rag.py` | `rag_query` 路由在 `set_generate_fn`/`set_reranker` 后新增 `engine.set_rewriter(llm_fn)` |

### RRF 多查询融合 — Reciprocal Rank Fusion 检索合并

| 文件 | 改动 |
|------|------|
| `backend/agent/rag/hybrid.py` | **新建** — 从 `example/hybrid.py` 提取 RRF 核心：`rrf_fuse()`（基于 rank 的多路融合，k=60）、`multi_query_search()`（顺序多路检索 + RRF 融合） |
| `backend/agent/rag/rag_engine.py` | `_multi_query_search` 从原始 cosine score 合并改为调用 `hybrid.multi_query_search`（RRF 融合），解决多查询分数尺度不可比问题 |
| `backend/agent/rag/__init__.py` | 导出 `rrf_fuse`、`multi_query_search` |

| 文件 | 改动 |
|------|------|
| `backend/core/hls_streamer.py` | **新建** — HLS 推流模块：管理 ffmpeg 子进程将 RTSP 流转为 HLS（.m3u8 + .ts 分片）；start/stop/status 接口；stderr 错误监控；全局单例 |
| `backend/main.py` | 新增 CORSMiddleware（跨域支持 HLS 文件）；新增 3 个 HLS API 路由（POST /api/hls/start, POST /api/hls/stop, GET /api/hls/status）；挂载 /hls 静态文件目录 |
| `web2/vite.config.ts` | 新增 `/hls` 代理到 `http://localhost:5000` |
| `web2/package.json` | 新增 `hls.js` 依赖 |
| `web2/src/components/LiveDetectTab.tsx` | RTSP/HLS 面板替换为视频源信息面板（显示 WS 直连/kmsgrab/MJPEG）；清理 HLS 相关死代码（hls.js/状态/函数/import） |
| `scripts/fb_ws_stream.py` | **新建→重写** — 板端一站式脚本：**自动启动官方 rknn_yolov8_cam** 并解析其 stdout 检测结果 → 发送 targets_stream；kmsgrab DRM 抓屏 → MJPEG → WS 推流；检测坐标自动归一化+旋转适配；支持 --no-yolo/--no-rotate |
| `backend/main.py` | **广播模式修复**：板端帧不再走 frame_queue（被多连接抢帧导致前端黑屏），改为直接广播到所有前端 WS 连接（`_frontend_clients` 集合管理）；断连时自动清理 |
| `web2/src/App.tsx` | 新增 video_frame 计数器；检测历史新增 `source` 字段；**实时检测去重**：同类型+同位置缺陷只记一次，检测中断 >5s 自动重置（换板） |
| `web2/src/types.ts` | `AlarmEvent` 新增 `source: 'live' | 'image'` 字段 |
| `web2/src/components/HistoryTab.tsx` | 检测记录显示来源标签：实时检测（绿色）/ 图片检测（蓝色） |
| `板端部署.md` | 新增方案 D（fb_ws_stream 直推）作为推荐方案；架构图更新；方案 C 标记为需 mediamtx；章节重新编号 |

### 模型下拉框 + 脚本清理

| 文件 | 改动 |
|------|------|
| `web2/src/components/ConfigTab.tsx` | 模型下拉框新增 `yolon8_best` 选项 |
| `web2/src/App.tsx` | 默认模型 `pcb_model` → `yolo26n` |
| `scripts/` | 删除 `bridge.py` `fbcapture.py` `generate_calibration_dataset.py` `test_detection.py`，保留 `fb_ws_stream.py` + `camera_stream.py` |
| `板端部署.md` | 方案对比表精简为当前方案 D，文件清单更新 | — Python 推理 + WebSocket 推流真实电路板实现

| 文件 | 改动 |
|------|------|
| `板端部署.md` | 全面改写为实际部署经验：添加 Python RKNNLite 推理方案、framebuffer RTSP 推流方案、常见问题（numpy 2.x+OpenCV 不兼容/量化后cls头被压死/摄像头旋转/queue满阻塞） |
| `backend/main.py` | 板端数据转发：接收 targets_stream/telemetry_metrics 写入 telemetry_queue 供前端消费；二进制帧写入 frame_queue；区分板端/前端 WS 连接（is_board 标志）；非阻塞入队 `_put_queue` 避免 queue.Full 卡死事件循环 |
| `backend/core/pipeline.py` | frame_queue/telemetry_queue 容量 8→32 |
| `web2/src/components/LiveDetectTab.tsx` | 移除假的 RTSP 模拟连接流程；改为 wsConnected 直接控制画面显示；接收 wsConnected/metrics props；接回真实 RTSP 配置面板和切换按钮 |
| `web2/src/App.tsx` | 新增 videoFrameUrl 状态 + video_frame 类型 WS 消息处理 + revokeObjectURL 防内存泄漏；向 LiveDetectTab 传入 wsConnected/metrics |
| `scripts/camera_stream.py` | **新建** − RV1126B 板端：摄像头采集 → NPU 推理（yolov8_best.rknn）→ targets_stream+JPEG 帧→ WebSocket 推流；YOLOv8 DFL 解码 + NMS 后处理（对齐官方 C 代码）；letterbox 预处理；自动断线重连 |
| `scripts/bridge.py` | **新建** − 桥接：运行官方 rknn_yolov8_cam 子进程 → 解析 stdout 检测框 → 独立抓帧画框 → WS 推流（因摄像头冲突未用） |
| `scripts/fbcapture.py` | **新建** − 截屏：ffmpeg 截 /dev/fb0 → MJPEG → WS 推流；Pillow 编码备选（因 numpy+OpenCV 不兼容） |

### 后端：板端数据转发 + 队列非阻塞

| 文件 | 改动 |
|------|------|
| `backend/main.py` | 板端 WS 消息识别转发；二进制帧→frame_queue；_put_queue 非阻塞入队；is_board 标志；reload=False 避免断连 |

### 前端：RV1126B 实时检测画面

| 文件 | 改动 |
|------|------|
| `web2/src/App.tsx` | videoFrameUrl 状态管理 + video_frame WS 消息处理 |
| `web2/src/components/LiveDetectTab.tsx` | 移除假 RTSP 连接模拟；wsConnected 直驱画面显示；接回真实 RTSP 配置和按钮 |

### 脚本：板端推理/桥接/截屏

| 文件 | 说明 |
|------|------|
| `scripts/camera_stream.py` | Python 直推方案：NPU 推理 + WS 推流（含正确 YOLOv8 DFL 解码） |
| `scripts/bridge.py` | 桥接方案：调用官方程序 + stdout 解析抓帧 |
| `scripts/fbcapture.py` | 截屏方案：ffmpeg 截 framebuffer → MJPEG WS 推流 |

## 2026-06-20

### 图片检测改造：批量检测 + 布局优化

| 文件 | 改动 |
|------|------|
| `ImageRecTab.tsx` | 改为批量图片检测（多图上传/列表/逐张检测/进度/统计汇总）；缺陷详情移至画面下方；空状态图标居中；移除 Gemini AI；检测参数移至左侧 |

### 侧边栏支持折叠

| 文件 | 改动 |
|------|------|
| `Sidebar.tsx` | 新增折叠/展开按钮，折叠时 `w-64` → `w-16`，仅显示图标 |

### 移除视频检测 Tab + 移除 Gemini AI 分析

| 文件 | 改动 |
|------|------|
| `Sidebar.tsx` | 移除「视频检测」菜单项 |
| `App.tsx` | 移除 `VideoRecTab` 导入和渲染 |
| `web2/src/components/ImageRecTab.tsx` | 移除 Gemini AI 分析（状态/回调/UI 卡片） |
| `web2/src/components/LiveDetectTab.tsx` | 移除 Gemini AI 分析（状态/回调/UI 卡片） |

### 新增：RAG 知识库页面

| 文件 | 改动 |
|------|------|
| `web2/src/types.ts` | `TabId` 新增 `rag`，移除 `video_rec` |
| `web2/src/components/RagTab.tsx` | **新建** — RAG 知识库组件：左侧文档管理（上传/删除/列表，localStorage 持久化），右侧检索对话（输入问题→模拟 RAG 检索→展示答案） |
| `web2/src/App.tsx` | 导入并渲染 `RagTab` |
| `web2/src/components/Sidebar.tsx` | 新增「RAG 知识库」菜单项（BookOpen 图标），移除 Play 图标 |

### 检测历史持久化 + 图片检测上报历史

| 文件 | 改动 |
|------|------|
| `web2/src/App.tsx` | `alarms` 初始化从 `localStorage` 加载 + `useEffect` 自动持久化；WS `targets_stream` 处理中生成告警记录；`ImageRecTab` 传入 `onDetect` 回调 |
| `web2/src/components/ImageRecTab.tsx` | 新增 `onDetect` prop，检测完成后回调上报历史 |

### 修复：检测框坐标错位

| 文件 | 改动 |
|------|------|
| `web2/src/utils/dataAdapter.ts` | 移除 `BASE_WIDTH`/`BASE_HEIGHT` 像素转换，x/y/width/height 直接保持 0-100% |
| `web2/src/components/LiveDetectTab.tsx` | 缺陷框定位从 `px` 改为 `%` |
| `web2/src/components/ImageRecTab.tsx` | 新增 `displayDims` 精确控制图片容器尺寸，替代 `aspectRatio`+`maxHeight` 组合；`object-fill` 替代 `object-contain` |

### 模型名更新

| 文件 | 改动 |
|------|------|
| `ImageRecTab.tsx` `VideoRecTab.tsx` `ConfigTab.tsx` | 默认模型名 `pcb_model` → `yolo26n`，显示名改为 `YOLO26n PCB 缺陷检测模型` |
| `backend/data/config.json` | `selectedModel` 从 `pcb_model` 改为 `yolo26n` |

### 布局优化：实时检测缺陷详情移至画面下方

| 文件 | 改动 |
|------|------|
| `web2/src/components/LiveDetectTab.tsx` | 缺陷详情从右侧面板移至视频画面下方，改用 5 格横排卡片网格布局（缺陷类型/英文名/置信度/位置/目标ID）；解除 rtspConnected 条件限制，模块始终可见 |

### 新增：板端部署指南

| 文件 | 说明 |
|------|------|
| `板端部署.md` | YOLO26n 模型部署到 RV1126B 完整指南：环境准备 → ONNX 导出 → RKNN 量化转换 → 板端推理程序 → 前后端对接 → 性能优化 → 故障排查 |

### 新增：AI 分析覆盖视频检测和实时检测

| 文件 | 改动 |
|------|------|
| `web2/src/components/VideoRecTab.tsx` | 右侧面板新增 Gemini AI 分析区域：分析按钮 + 报告显示（视频处理完成可用） |
| `web2/src/components/LiveDetectTab.tsx` | 右侧面板新增 Gemini AI 分析区域；缺陷详情从右侧面板移至画面下方，改用卡片网格布局 |

### 新增：一键启动脚本

| 文件 | 说明 |
|------|------|
| `start.py` | 项目根目录一键启动前后端：`python start.py`，同时启动 FastAPI + Vite，实时输出日志，Ctrl+C 统一关闭 |

### 后端改造完成（阶段一 + 阶段二）

后端已从交通检测全面改造为 PCB 缺陷检测：

| 文件 | 改动 |
|------|------|
| `backend/models.py` | **重写** — `SystemConfig` 新增 PCB 配置字段（saveResults/retentionDays/jpegQuality/maxFps/npuMode/tempWarning/reviewThreshold/consecutiveAlerts），`DetectionTarget` 移除交通字段（speed/distance/licensePlate/warning/pedestrianStatus/path），新增 `defectType`/`confidence`；移除 `WarningType`/`TrafficHistory` |
| `backend/core/pipeline.py` | **重写** — `_CLASS_NAMES` 替换为 6 类 PCB 缺陷映射；移除 `Estimator` 调用/轨迹线/车道逻辑；简化推送数据（仅 id/type/className/x/y/width/height/confidence） |
| `backend/core/estimator.py` | **已删除** — 速度/车距估算模块（PCB 不需要） |
| `backend/core/hardware.py` | 简化为基础 FPS/CPU/内存采集 |
| `backend/core/yolo_engine.py` | `TrackedObject` 移除 speed/distance/warning/path 字段 |
| `backend/config.py` | 默认配置适配新 `SystemConfig`；load() 自动过滤旧字段 |
| `backend/main.py` | `_FRONTEND_TO_BACKEND` 精简；WS 推送/seek/image_detect 全链路适配 PCB 格式；Gemini 报告改为 PCB 主题；移除 `_CLASS_NAMES` 改为引用 pipeline 的 |
| `backend/processor.py` | 移除轨迹线绘制 |
| `backend/store/alarm_store.py` | 简化告警表结构（移除 license_plate/speed/distance/warning_type） |
| `backend/store/history_store.py` | 简化为日检测数统计 |
| `backend/data/config.json` | 更新为新 PCB 配置字段 |

### 重构：系统配置页（ConfigTab）

| 文件 | 改动 |
|------|------|
| `web2/src/types.ts` | `SystemConfig` 重新设计：移除 `confidence`/`iouThreshold`（各 Tab 独立调节），新增 `saveResults`/`retentionDays`/`jpegQuality`（检测存储）、`maxFps`/`npuMode`/`tempWarning`（板端参数）、`reviewThreshold`/`consecutiveAlerts`（告警通知） |
| `web2/src/components/ConfigTab.tsx` | **重写** — 布局分为四大区块：模型配置、检测存储（保存开关/保留天数/图像质量）、RV1126B 板端参数（帧率/NPU模式/温度阈值）、告警与通知（复查阈值/连续告警数）+ 系统信息面板 |
| `web2/src/App.tsx` | 默认配置同步更新为新字段 |

### 文档同步

| 文件 | 改动 |
|------|------|
| `前端结构梳理.md` | 全面重写为当前 PCB 前端架构：新增 LiveDetectTab 组件说明、RV1126B 实时检测流程、6 色缺陷映射、更新目录/组件树/类型定义/交互流程/改造状态表 |
| `后端结构梳理.md` | 同步为当前后端状态：明确标注「后端尚未改造」，新增 8 项待改造任务（含 RTSP 管线适配），更新数据流图中加入 RV1126B 实时检测链路 |

### 新增：RV1126B 板端实时 PCB 缺陷检测

| # | 文件 | 改动 |
|---|------|------|
| — | `web2/src/types.ts` | `TabId` 新增 `live_detect` |
| — | `web2/src/components/LiveDetectTab.tsx` | **新建** — RV1126B 实时检测组件：RTSP 视频流连接、6 色缺陷框叠加、板端状态监控（NPU/温度/FPS）、实时缺陷统计列表 |
| — | `web2/src/App.tsx` | 导入并渲染 `LiveDetectTab` |
| — | `web2/src/components/Sidebar.tsx` | 新增「RV1126B 实时检测」菜单项（Monitor 图标，排第一） |

### 转型：交通检测 → PCB 缺陷检测

- 模型从 15 类交通模型改为 6 类 PCB 缺陷模型（missing_hole, mouse_bite, open_circuit, short, spur, spurious_copper）
- 详见 `progress.md` 完整可执行计划

### 前端改造（阶段三）

| # | 文件 | 改动 |
|---|------|------|
| 7 | `web2/src/types.ts` | `TabId` 移除 `camera`；`SimulatedTarget` 移除 `speed`/`distance`/`plate`/`lane`/`trajectory`/`alarmLevel`，新增 `confidence`/`defectType`；`SystemConfig` 移除 `speedLimit`/`showTracks`/`rtspUrl`；`AlarmEvent` 精简 |
| 8 | `web2/src/App.tsx` | 移除 `CameraTab` 导入和渲染；移除 `processViolationChecks` 交通违规逻辑；默认 Tab 改为 `image_rec`；配置默认值适配 PCB |
| 9 | `web2/src/components/ImageRecTab.tsx` | 颜色映射改为 6 类 PCB 缺陷色（红/橙/紫/蓝/绿/黄）；检测统计改为缺陷分类统计；详情面板显示缺陷名称和置信度；UI 文字改为 PCB 上下文 |
| 10 | `web2/src/components/VideoRecTab.tsx` | 移除 `showTracks` 轨迹线开关；UI 文字改为 PCB；模型选项改为 PCB 模型 |
| 11 | `web2/src/components/Sidebar.tsx` | 移除 `camera` 菜单项；菜单改为 PCB 图片检测/视频检测/检测历史/系统配置/个人中心 |
| 12 | `web2/src/components/CameraTab.tsx` | **删除** — RTSP 摄像头组件，PCB 不需要 |
| — | `web2/src/components/Header.tsx` | 标题改为「PCB 缺陷检测系统 v1.0.0」 |
| — | `web2/src/components/HistoryTab.tsx` | 移除车牌/速度/过滤类型；UI 改为缺陷检测记录 |
| — | `web2/src/components/ConfigTab.tsx` | 移除速限/RTSP/轨迹线开关；精简为置信度+IOU+模型 |
| — | `web2/src/components/UserTab.tsx` | 文字从交通改为 PCB 上下文 |
| — | `web2/src/components/LoginScreen.tsx` | 从 RV1126B 交通平台改为 PCB 缺陷检测系统 |
| — | `web2/src/utils/dataAdapter.ts` | 移除交通字段映射，简化 PCB 缺陷数据适配 |

## 遗留

- 模型文件尚未放入：需将 `.pt` 模型文件放入 `model/pcb_model.pt`（配置已默认指向此路径）
- `start.py` 需要安装 Python 依赖：`pip install fastapi uvicorn opencv-python python-multipart psutil ultralytics`

### 2026-07-14：LangSmith RAG 评估链路

| 文件 | 改动 |
|---|---|
| `rag_eveal/test1.py` | 重构为固定数据集同步、隔离无泄漏索引、检索指标、LLM 裁判指标、本地基线与结果导出的一体化评估脚本 |
| `backend/agent/rag/rag_engine.py` | 检索结果按文档父块去重；无 reranker 时也严格遵守 `top_k` |
| `backend/agent/rag/rewriter.py` | 中文技术问题至少生成一条英文技术检索式，改善英文知识库召回 |
| `LANGSMITH.md` | 增加评估运行方式、指标说明和外部数据传输边界 |

验证：Python 编译通过；32 条本地 TF-IDF 基线为 Hit@1 `0.2500`、Hit@3 `0.3750`、MRR `0.3021`、上下文精度 `0.1458`。完整 LangSmith 运行因需要向外部端点发送评估内容，待用户明确确认后执行。

#### 评估运行报错修复

- `rag_engine.py`：为 TF-IDF 首次拟合增加实例级可重入锁和二次状态检查，并在局部变量中完整拟合后原子更新实例状态，修复 LangSmith 并发任务触发的 `NotFittedError`。
- `rag_eveal/test1.py`：裁判解析支持纯 JSON、Markdown JSON 代码块以及“推理说明 + JSON”；完全无 JSON 时写入 `judge_parse_error` 分类反馈，不再抛出 evaluator 异常。
- `llm_service.py`：LLM 调用支持单独指定 temperature；裁判使用 `temperature=0` 和 1024 token 输出预算，减少只返回推理过程、未生成最终 JSON 的情况。
- target 运行失败时，检索与回答 evaluator 统一写入 `target_error`，避免对失败样本继续调用裁判模型并产生级联报错。
- LangSmith 上传改为显式 `upload_results=True`；完整评估固定归属 `rag_evaluation`，冒烟评估归属 `rag_evaluation-smoke-N`，结束时打印 Experiment URL。
- 验证：8 线程同时执行 32 次首次 TF-IDF 检索通过；裁判 JSON 提取及无 JSON 降级测试通过；本地 32 条检索基线复测通过。

#### 评估质量、混合检索与成本追踪改进

- `rag_evaluation.csv`：新增 `relevant_doc_ids` 和 `reference_answer`；32 条父/子章节关系及中文标准答案均已校验。
- `test1.py`：LangSmith dataset outputs 改为 `relevant_doc_ids + reference_answer`；Hit@K、MRR 和上下文精度接受多个相关章节。
- `rag_engine.py`：新增 jieba 词级与字符 n-gram 加权检索；有向量后端时通过 RRF 融合向量和稀疏候选。
- `hybrid.py`：RRF 改用文档/块稳定键，避免 Chroma 本地 id 为 0 时错误合并不同结果。
- `config.py` / `backend/agent/.env`：启用混合检索和 LLM reranker，字符通道权重为 `0.35`。
- `llm_service.py`：自定义 OpenAI 兼容 HTTP 调用加入脱敏 LangSmith `llm` span，解析 usage token，并支持供应商 cost 或每百万 token 费率计算。
- `rag_eveal/.env.langsmith.example`：增加 token 价格和可选多语言 embedding 配置模板。
- 验证：CSV 33×8 表格检查通过；32 条标签引用均有效；本地新基线 Hit@1 `0.2812`、Hit@3 `0.4375`、MRR `0.3490`；并发混合检索、RRF 去重和 usage/cost 计算测试通过。
