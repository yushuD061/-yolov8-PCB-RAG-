# 代码概要说明

> 本项目为 **PCB 缺陷检测系统**，覆盖 6 类缺陷（missing_hole / mouse_bite / open_circuit / short / spur / spurious_copper）。整个工程的代码统一参考 `backend/agent/rag_engine.py` 的结构范式：
>
> **「先定义数据的类型结构，再设计详细的业务逻辑」**
>
> - 数据结构（dataclass / Pydantic / interface / const 枚举）集中在文件顶部或独立 `types`/`models` 文件中；
> - 业务逻辑模块依赖明确的类型契约，便于阅读、测试与跨端对齐；
> - 模块间通过类型化对象传递（如 `RagChunk`、`RetrievalHit`、`SystemConfig`、`TrackedObject`、`SimulatedTarget`）。

---

## 1. 项目结构总览

```
test4/
├── backend/         ├─ FastAPI 后端 + YOLO 推理管线 + RAG 智能体
│   ├── main.py      FastAPI 入口：WS / REST 路由 + 生命周期
│   ├── models.py     Pydantic 数据模型（与前端 types.ts 对齐）
│   ├── config.py     SystemConfig 加载/保存/监听
│   ├── processor.py  离线视频逐帧推理 + 烧录
│   ├── core/         推理管线核心
│   ├── store/        SQLite 持久化（告警 / 历史）
│   ├── agent/        RAG 引擎 + Agent 配置
│   ├── utils/        工具（环形缓冲 / 透视变换）
│   └── data/         运行时数据目录（config.json / *.db / hls / uploads）
├── scripts/         板端推流脚本（RV1126B）
│   ├── camera_stream.py    RKNNLite 直接推理 + WS 推流
│   └── fb_ws_stream.py     KMS 抓屏 + 官方程序 stdout 解析
├── web2/            React + TypeScript + Vite 前端
│   ├── src/types.ts        跨端类型契约
│   ├── src/utils/          WS 客户端 + 适配器
│   ├── src/App.tsx         全局状态根
│   └── src/components/     各功能页 Tab
├── model/           yolo26n.pt / yolo26n.onnx / yolon8_best.pt
├── image/           样例 PCB 缺陷图片
└── video_output/    离线处理后视频输出
```

### 技术栈
- 后端：Python 3.11+ / FastAPI / Ultralytics YOLO / OpenCV / SQLite / scikit-learn / jieba
- 板端：Python / RKNNLite / ffmpeg / websockets
- 前端：React 18 / TypeScript / Vite / TailwindCSS / lucide-react

---

## 2. 统一编码约定（数据先行）

1. **每个模块文件顶部先列出数据结构**
   - Python 后端：优先 `@dataclass`（逻辑结构）或 Pydantic `BaseModel`（API/持久化契约）。
   - 前端：放 `src/types.ts`，组件内私有类型就近定义在文件顶部 `interface`。
   - 板端脚本：模块顶部用常量 + 字典定义 LABELS / 配置；流水线数据用 plain dict 但保持稳定键名。

2. **结构与逻辑用注释分隔**
   使用 `# ═══════════════════ 数据结构 ═══════════════════` / `# ═══════════════════ XX 逻辑 ═══════════════════` 这类横幅分隔（参考 rag_engine.py）。

3. **跨端契约以类型为主**
   - 后端 `models.py` ↔ 前端 `src/types.ts`（字段名差异由 `dataAdapter.ts` 适配）。
   - WS 消息体：`{type, data}` 或 `{id, method, channel, data}`；REST 统一 JSON。

4. **数据流向**
   `板端/本地资源 → 视频源 → YOLO 推理 → 检测对象 → WS/REST → 前端状态 → UI`

---

## 3. 后端模块概要

### 3.1 `backend/models.py` — 数据模型层 ★核心契约

**职责**：定义与前端 `types.ts` 对齐的 Pydantic 模型，供 `main.py`/`core`/`store` 复用。

| 类型 | 字段要点 | 用途 |
|---|---|---|
| `SystemConfig` | detectionConfidence / iouThreshold / selectedModel / saveResults / retentionDays / jpegQuality / maxFps / npuMode / tempWarning / reviewThreshold / consecutiveAlerts / enableInferenceTelemetry | 全局配置 |
| `HardwareTelemetry` | fps / npuUtilization / cpuUtilization / memoryUsed / memoryTotal / socTemperature / inferenceLatency / status | 遥测数据 |
| `DetectionTarget` | id / type="defect" / className / confidence / x / y / width / height（0-100%） | WS 检测目标 |
| `AlarmRecord` | id / timestamp / targetId / type / message | 告警记录 |

**逻辑要点**：纯数据，无方法。供 `config.ConfigManager`、`core.hardware`、`core.pipeline`、`store.alarm_store`、`main` 路由引用。

---

### 3.2 `backend/config.py` — 配置管理

**数据结构**：
- `_CONFIG_PATH`（`backend/data/config.json`）
- `_DEFAULT_CONFIG = SystemConfig()` 兜底

**逻辑**：
- `ConfigManager`：`load()` → `get()` → `update(partial)` → `_save()`；`on_change(listener)` 监听器列表 通知 `pipeline.reconfigure()`。
- 过滤非法字段（仅取 `SystemConfig.model_fields.keys()`）。

---

### 3.3 `backend/core/` — 推理管线核心

#### 3.3.1 `yolo_engine.py`
**数据结构**：
- `TrackedObject`：id / x / y / w / h / class_id / confidence（0-100% 归一化）
- `_names: dict[int, str]` 类别映射

**逻辑**：
- `YoloEngine.load(model_path)` 懒加载 Ultralytics YOLO `.pt`。
- `track(frame, conf_threshold, iou_threshold, classes)` → `model.track(persist=True)` → 像素坐标转 0-100% → 列表 `TrackedObject`。
- `plot(frame)` 返回最近一次推理的标注帧；`class_names` 属性。

#### 3.3.2 `video_source.py`
**数据结构**：内部状态 `source_type ∈ {rtsp|video|image}`, `RingBuffer`, `_backoff`。

**逻辑**：
- `open(uri, source_type)`：图片直接 `cv2.imread`；视频/RTSP 走 `_create_capture`（优先 MSMF，避开 FFmpeg 多线程断言）。
- `read()`：失败重连指数退避；视频结尾自动重开循环（MSMF 不支持 seek）。
- `seek() / reset() / close()`；属性 `is_opened` / `source_type`。

#### 3.3.3 `hardware.py`
**数据结构**：`_fps_history: list[float]`, `_last_inference_time`。

**逻辑**：`record_inference(elapsed_ms)` / `record_frame()` 计算 FPS；`collect()` 用 `psutil` 采集 CPU/内存，NPU/温度 PC 环境置 0；返回 `HardwareTelemetry`。

#### 3.3.4 `pipeline.py` — 后台线程主循环 ★
**数据结构**：
- `_CLASS_NAMES`（6 类缺陷 id→name 映射）
- 状态：`engine: YoloEngine`、`video_source: VideoSource`、`hardware: HardwareCollector`
- 队列：`telemetry_queue(max=32)`、`frame_queue(max=32)`

**逻辑**：
- `start(config)` 启动守护线程 → `_async_loop()`：
  - `video_source.read()` → 缺帧 sleep
  - `engine.track()` → 构建 `targets_data`（dict，对齐主流协议）
  - `hardware.collect()` → 入 `telemetry_queue`
  - 节流（0.2s）将 `engine.plot()` JPEG 入 `frame_queue`
  - 帧率压制（`max 50ms`）
- `reconfigure(config)`：模型变更时 `engine.load()`。
- `stop()` 优雅停。

#### 3.3.5 `hls_streamer.py`
**数据结构**：内部 subprocess、`status` dict（`running / rtspUrl / startedAt / error / outputDir / playlistUrl`）。

**逻辑**：`start(rtsp_url)` 启 ffmpeg `RTSP→HLS (copy 编码)`，stderr 监控线程捕获错误；`stop()` 终止并清空目录。`get_hls_streamer()` 全局单例。

---

### 3.4 `backend/processor.py` — 离线视频处理

**数据结构**：`_CLASS_NAMES` 同 pipeline；状态 `_model / _progress / _error / _output_path`。

**逻辑**：`start(video_path, config, progress_callback, done_callback)` 守护线程；逐帧 `model.track()` → `results[0].plot()` → `VideoWriter(mp4v)` 写出 → 每 5% 回调进度；完成回调 `done_callback(success, error, url)`。

---

### 3.5 `backend/store/` — 持久化

#### 3.5.1 `alarm_store.py`
**数据结构**：SQLite 表 `alarms(id, timestamp, target_id, type, message)`；`_dedup_cache: dict[int,float]`。

**逻辑**：`append(AlarmRecord)` 同 target 30s 去重；`list(limit)` / `clear()` / `stats()` 按缺陷类型解析 `message` 正则 → `{total, by_type, first_at, last_at}`。

#### 3.5.2 `history_store.py`
**数据结构**：SQLite 表 `daily_stats(date, total_detections)`（与 alarm_store 共用 `alarms.db`）。

**逻辑**：`accumulate(n)` 按日累加；`get_7days()` 返回近 7 天 `[{date, totalDetections}]`。

> **建议**：两个 store 共用同一 DB 文件，可抽 `BaseStore.connect/close` 基类统一连接管理，并把 `_DB_PATH` 提到 `config`。

---

### 3.6 `backend/agent/` — RAG 智能体 ★范式样板

#### 3.6.1 `config.py`
**数据结构**：`@dataclass AgentConfig`（llm_endpoint / llm_api_key / llm_model / rag_chunk_size / rag_chunk_overlap / rag_top_k）。

**逻辑**：`load_agent_config()` 先读 `.env`（`dotenv_values`），再让 `os.environ` 覆盖；`get_agent_config()` 模块级单例。

#### 3.6.2 `rag_engine.py` ★本工程结构样板
**数据结构**（顶部集中定义）：
- `@dataclass RagChunk`：id / doc_id / doc_name / chunk_index / content / parent_content
- `@dataclass RetrievalHit`：chunk / score / source

**逻辑**（分块清晰）：
1. `RecursiveSplitter.split` 段落→句子→定长回退
2. `RagStore` SQLite 读写 `rag_chunks` / `rag_docs`，线程锁
3. `RagEngine`：
   - `ingest(text, doc_name)` 父子两级切分 → `store.add_chunks` → 置 `_dirty`
   - `query(question, extra_context)` → `_search()` Top-K → `_compose()` LLM 合成（缺 LLM 降级为检索摘要）
   - `_ensure_tfidf()` 惰性构建 `TfidfVectorizer` + `jieba` tokenizer + 余弦相似度
   - `set_generate_fn(fn)` 外部注入 LLM 函数
4. `get_rag_engine()` 全局单例，由 `main.py` 注入 LLM API 调用器。

> **此文件是新模块的样板**：dataclass 先行 → 切分 → 存储 → 引擎 → 全局单例。

---

### 3.7 `backend/utils/`

- `ring_buffer.py`：`RingBuffer(capacity)` 线程安全 FIFO：`push / latest / get_all / clear / __len__`。
- `perspective.py`：`PerspectiveTransform` 维护 `_matrix / _inverse`（OpenCV 单应性）；`calibrate / warp / pixel_to_world / world_to_pixel`。

---

### 3.8 `backend/main.py` — FastAPI 入口 ★

**数据结构**：
- 全局单例：`config_mgr / pipeline / processor / alarm_store / history_store`
- `_process_tasks: dict[task_id, dict]` 离线任务表
- `_frontend_clients: set[WebSocket]` 前端连接集合
- `_FRONTEND_TO_BACKEND` 配置字段翻译表

**逻辑**：
- `lifespan`：启动时 `config_mgr.load() → store.connect() → pipeline.start(cfg)`，注入 `on_change` 回调；关闭时反向停。
- WS `/ws`：纯异步循环
  - 推送 `telemetry` / `video_frame` 给前端
  - 板端二进制帧 → 广播给所有 `_frontend_clients`
  - 板端 JSON（`targets_stream` / `telemetry_metrics`）→ 转发前端 + 同步写 `AlarmStore`
  - 前端 JSON 路由：`subscribe`、`CONF_UPDATE`、`get list factory history`、`upload / upload:done`、`switch / seek / reset`
- REST：
  - `POST /api/upload`、`GET /api/process/{task_id}`（离线处理任务）
  - `POST /api/detect/image`（单图 YOLO 检测，返回归一化坐标）
  - RAG：`/api/rag/{upload,query,documents,documents/{id},reindex}`
  - HLS：`/api/hls/{start,stop,status}`
  - `/api/alarms/report`（前端图片检测上报）、`/api/gemini/analyze`（预设报告）
  - `/debug*`、`/api/health`、`/api/ping`
- 静态挂载：`video_output/`、`hls/`、`web2/dist/`（构建产物）。

---

## 4. 板端脚本（scripts/）

### 4.1 `scripts/camera_stream.py`
**数据结构**：模块顶部常量 `MODEL_PATH / LABELS / CAMERA / IMG_SIZE / CONF / IOU / WS_URL`。

**逻辑**：`RKNNLite` 加载 NPU 模型 → `cap.read()` → `preprocess`（旋转+letterbox） → `rknn.inference` → `postprocess`（DFL 解码 + NMS + 0-100% 归一化）→ WS 推送 `targets_stream` + JPEG 帧 + 周期 `telemetry_metrics`。

### 4.2 `scripts/fb_ws_stream.py`
**数据结构**：argparse `args`、`FB_W/FB_H/OUT_W/OUT_H`、`latest_detections: list[dict]` + `det_lock`。

**逻辑**：
- `run_yolo()` 拉起官方 `rknn_yolov8_cam` 子进程，`yolo_stdout_reader` 按行解析（`parse_yolo_line` 正则两种格式）→ 归一化 `det_to_target`
- `run_ffmpeg()` kmsgrab + transpose → image2pipe MJPEG
- `ws_send_loop`：`extract_jpeg` 边界匹配 SOI/EOI 提帧 → send + 周期遥测
- 断线重连 3s。

---

## 5. 前端（web2/）

### 5.1 `src/types.ts` — 跨端类型契约 ★
- `TabId = 'live_detect'|'image_rec'|'history'|'config'|'user'|'rag'`
- `TargetType = 'defect'`
- `DEFECT_NAMES_CN` 6 类中英映射 + `getDefectNameCn`
- `SimulatedTarget`：id/type/className/x/y/width/height/confidence/defectType/color
- `TelemetryMetrics`：fps/npu/cpu/memUsed/memTotal/temp/latency/selectedModel
- `AlarmEvent`：id/timestamp/type/targetId/description/source('live'|'image')
- `SystemConfig`

### 5.2 `src/utils/`
- `wsClient.ts`：`WSClient` 单例。状态字段 `socket / handlers / reconnectTimer / isConnecting / url`；方法 `connect / send(channel,method,data) / subscribe(handler)→unsubscribe / triggerHandlers / scheduleReconnect`。
- `dataAdapter.ts`：`normalizeTargets(raw[])→SimulatedTarget[]`、`normalizeTelemetry(raw)→TelemetryMetrics` 处理前后端字段差异（npu/npuUtilization、cpu/cpuUtilization、socTemperature/temp 等）。

### 5.3 `src/App.tsx` — 全局状态根 ★
**State**：`currentUser / activeTab / wsConnected / isDarkMode / config / targets / selectedTargetId / alarms(localStorage 持久化) / logs / metrics / videoFrameUrl`；`dedupRef`（5s 换板去重）。

**逻辑**：WS 订阅统一分发 `status_change / targets_stream / telemetry_metrics / log_broadcast / video_frame`；`updateConfig` 通过 WS `CONF_UPDATE` 或本地降级；子 Tab props 注入。

### 5.4 `src/components/`
| 组件 | 职责 | 主要 props |
|---|---|---|
| `Header` | 顶部状态栏：连接/模型/FPS/NPU/温度/主题切换 | metrics/wsConnected/isDarkMode/setIsDarkMode |
| `Sidebar` | 左侧导航 Tab | activeTab/setActiveTab |
| `LoginScreen` | 登录页 | onLogin/isDarkMode/setIsDarkMode |
| `LiveDetectTab` | 实时检测画布（视频帧 + 框叠加） | targets/selectedTargetId/setSelectedTargetId/videoFrameUrl/wsConnected/metrics/pushLog |
| `ImageRecTab` | 单图上传 + `/api/detect/image` + 检测后回调 | pushLog/onDetect |
| `HistoryTab` | 告警列表 + 清空 | alarms/clearAlarms |
| `ConfigTab` | 系统配置表单（阈值/模型/存储） | config/updateConfig |
| `RagTab` | RAG 知识库：文档上传/列表/问答 + LLM API 配置（localStorage） | pushLog |
| `UserTab` | 终端日志 + 当前用户 + 退出 | logs/currentUser/onLogout |

**RagTab 内部类型**（顶部 interface）：`RagDocument`、`RagMessage('user'|'assistant')`、`LlmApiConfig{endpoint,apiKey,model}`、`DEFAULT_LLM_CONFIG`。

---

## 6. 数据流与协议

```
板端 camera_stream.py / fb_ws_stream.py
        │  WS /ws  (targets_stream / telemetry_metrics / JPEG)
        ▼
backend/main.py  ── 路由 ──┬── Pipeline (telemetry_queue / frame_queue)
                          ├── AlarmStore / HistoryStore
                          ├── RagEngine (SQLite + TF-IDF + LLM)
                          └── HlsStreamer (ffmpeg→/hls/*)
        │  WS + REST
        ▼
web2 (WSClient 单例 + dataAdapter 归一化)
        │
        ▼
React State (App.tsx) → 各 Tab 组件
```

**关键契约**：
- 检测框坐标统一为 **0-100% 相对中心 + 宽高**（板端、后端 pipeline、`/api/detect/image`、前端 `dataAdapter` 一致）。
- WS 文本消息：`{type, data}`（推送）/ `{id, method, channel, data, ?}`（请求）；二进制即 JPEG 视频帧。
- 配置字段：前端 camelCase，后端 `SystemConfig` 多为 camelCase（因 Pydantic 显式命名）；少量经 `_FRONTEND_TO_BACKEND` 翻译。

---

## 7. 数据类型先行 — 改造与对齐建议

> 目标：让「先定义数据结构，再写逻辑」成为所有模块的硬约定。

1. **板端脚本**（`camera_stream.py` / `fb_ws_stream.py`）目前用 bare dict 与裸常量。建议：
   - 顶部抽 `@dataclass Detection`（class_name/x1/y1/x2/y2/confidence）与 `Target`（已发往 WS 的归一化形态）。
   - 将 LABELS / IMG_SIZE / CONF / IOU / WS_URL 抽到独立 `board_config.py`。

2. **`core/video_source.py` / `core/hardware.py`** 当前内部状态散落。建议显式 `@dataclass VideoSourceState`（uri/source_type/backoff）、`@dataclass HardwareSnapshot`，便于单测断言。

3. **`store/alarm_store.py` 与 `store/history_store.py`**：
   - 抽 `AlarmStats`、`DailyStat` dataclass 作为 `stats()` / `get_7days()` 的返回类型，替代裸 dict。
   - 抽 `BaseStore`（connect/close/`_db_path`）减少共用 DB 文件的重复。

4. **`main.py`**：将 `_process_tasks` 升级为 `@dataclass ProcessTask`；将 WS 路由分文件（`routes/ws.py` / `routes/rag.py` / `routes/hls.py` / `routes/detect.py`），数据契约集中到 `models.py` / 新增 `schemas.py`。

5. **前端**：
   - 将 `RagTab` 内联的 `RagDocument / RagMessage / LlmApiConfig` 上提到 `src/types.ts`，与后端 `RagStore.get_docs()` 返回结构对齐。
   - 为 WS 协议生成 `WSPushMessage` / `WSRequest` 的 discriminated union，替代 `data: any`。

6. **统一文档化类型**：本概要说明中标注 ★ 的文件是契约/范式核心，改动需同步审阅前后端 + 板端三处。

---

*文档随项目演进动态维护。新增模块请遵循 rag_engine.py 的「数据结构 → 详细逻辑 → 全局单例」三段式。*