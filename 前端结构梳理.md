# 前端结构梳理

> PCB 缺陷检测系统前端 — React 19 + Vite 6 + TypeScript 5.8  
> **核心原则**：所有渲染使用纯 DOM / CSS，不依赖 `<canvas>`。检测框用绝对定位 `<div>` 实现，交互由原生事件处理。

---

## 1. 技术栈

| 类别 | 选型 | 说明 |
|------|------|------|
| 框架 | React 19 + TypeScript 5.8 | 全部组件函数式 + Hooks |
| 构建 | Vite 6 | 开发热更新，生产打包 |
| 样式 | Tailwind CSS 4 | 暗色主题（`bg-[#121214]` 等自定义色板） |
| 图标 | lucide-react | 统一 SVG 图标库（含 Monitor 等） |
| 动画 | Loader2 旋转指示器 | 仅用于处理中状态 |
| 通信 | WebSocket 单例 | `wsClient.ts` 封装，自动重连 |
| 渲染 | **纯 DOM** | 不引入 Canvas 2D / WebGL |

---

## 2. 目录结构

```
web2/src/
├── main.tsx                    # ReactDOM 渲染入口
├── App.tsx                     # 根组件：全局状态 + WS 订阅 + 路由分发（5 个 Tab）
├── types.ts                    # 所有 TypeScript 类型定义
├── index.css                   # Tailwind + 全局样式变量
├── utils/
│   ├── wsClient.ts             # WebSocket 单例（连接/订阅/重连）
│   └── dataAdapter.ts          # 后端数据 → 前端类型转换（PCB 缺陷适配）
└── components/
    ├── Header.tsx              # 顶部遥测栏（FPS / 连接状态）
    ├── Sidebar.tsx             # 左侧导航（6 个 Tab）
    ├── LiveDetectTab.tsx       # RV1126B 板端实时 PCB 缺陷检测（RTSP 流）
    ├── ImageRecTab.tsx         # 图片 PCB 缺陷检测（上传 → YOLO → 缺陷框）
    ├── HistoryTab.tsx          # 缺陷检测历史列表（localStorage 持久化）
    ├── ConfigTab.tsx           # 系统参数配置
    ├── RagTab.tsx              # RAG 知识库（文档上传+检索对话）
    ├── LoginScreen.tsx         # 登录界面
    └── UserTab.tsx             # 个人中心
```

---

## 3. 组件树

```
<App>                               ← 全局状态 + WS 生命周期
├── <Header />                      ← metrics props
├── <Sidebar />                     ← activeTab + setActiveTab
└── <main>
    ├── <LiveDetectTab />           ← RV1126B 实时检测（RTSP 流 + 实时缺陷框）
    ├── <ImageRecTab />             ← 图片检测（上传 → YOLO → 缺陷框）
    ├── <HistoryTab />              ← 检测历史 + 清空（localStorage 持久化）
    ├── <ConfigTab />               ← 全局系统配置（模型 / 存储 / 板端 / 告警）
    ├── <RagTab />                  ← RAG 知识库（文档上传 + 检索对话）
    └── <UserTab />                 ← 个人信息 + 日志
```

---

## 4. 状态管理

全部状态集中在 `App.tsx`，通过 props 下传。无 Redux / Zustand。

| 状态 | 类型 | 来源 | 消费组件 |
|------|------|------|---------|
| `targets` | `SimulatedTarget[]` | WS `targets_stream` | LiveDetectTab, ImageRecTab |
| `selectedTargetId` | `number\|null` | 本地（点击框） | LiveDetectTab, ImageRecTab |
| `metrics` | `TelemetryMetrics` | WS `telemetry_metrics` | Header |
| `wsConnected` | `boolean` | WS `status_change` | Header |
| `config` | `SystemConfig` | 本地 + WS `CONF_UPDATE` | ConfigTab |
| `alarms` | `AlarmEvent[]` | WS `targets_stream` + ImageRecTab 回调 → 持久化到 localStorage | HistoryTab |
| `logs` | `string[]` | WS `log_broadcast` + 本地 | UserTab |

---

## 5. 类型定义 (`types.ts`)

```typescript
type TabId = 'live_detect' | 'image_rec' | 'video_rec' | 'history' | 'config' | 'user';

interface SimulatedTarget {
  id: number;
  type: 'defect';
  className: string;              // 缺陷英文名（如 "missing_hole"）
  x: number; y: number;           // 框中心像素坐标（1100×620 基准）
  width: number; height: number;
  confidence: number;             // 0-1
  defectType: string;             // 缺陷类型（同 className）
  color: string;
}

interface TelemetryMetrics {
  fps: number;
  npu: number;                    // %
  cpu: number;                    // %
  memUsed: number;                // MB
  memTotal: number;               // MB
  temp: number;                   // °C
  latency: number;                // ms
  selectedModel: string;
}

interface SystemConfig {
  selectedModel: string;          // 推理模型
  // 检测存储
  saveResults: boolean;           // 自动保存检测结果
  retentionDays: number;          // 结果保留天数
  jpegQuality: number;           // 保存图片质量 50-100
  // 板端配置
  maxFps: number;                // 最大推理帧率 15-60
  npuMode: 'powersave' | 'balanced' | 'performance';
  tempWarning: number;           // 温度告警阈值 °C
  // 告警与通知
  reviewThreshold: number;       // 复查置信度阈值
  consecutiveAlerts: number;     // 连续缺陷告警数
}
```

---

## 6. 纯 DOM 渲染机制

### 6.1 实时检测框

实时检测中的缺陷框与图片检测使用相同的纯 DOM 绝对定位技术：

```tsx
<div className="targets-overlay absolute inset-0 pointer-events-auto w-full h-full">
  {targets.map((target) => {
    const color = getDefectColor(target.className);
    return (
      <div
        key={target.id}
        className={`target-box ${selectedTargetId === target.id ? 'selected' : ''}`}
        style={{
          left: `${target.x - target.width / 2}px`,
          top: `${target.y - target.height / 2}px`,
          width: `${target.width}px`,
          height: `${target.height}px`,
          '--box-color': color,
        } as React.CSSProperties}
        onClick={(e) => { e.stopPropagation(); setSelectedTargetId(target.id); }}
      >
        <span className="corner tl" />
        <span className="corner tr" />
        <span className="corner bl" />
        <span className="corner br" />
        <span className="absolute -top-6 left-0 text-[9px] px-1.5 py-0.5 rounded font-bold whitespace-nowrap text-white"
              style={{ backgroundColor: color }}>
          {defectNameCn} {Math.round(confidence * 100)}%
        </span>
      </div>
    );
  })}
</div>
```

检测框使用四角标线样式（`.corner.tl/tr/bl/br`），选中时显示阴影高亮 + 半透明底色填充。

### 6.2 图片检测框

```tsx
<div className="relative" style={{ aspectRatio: `${imgW}/${imgH}` }}>
  <img src={image} className="w-full h-full object-contain" />
  {detections.map((d, i) => (
    <div key={i} onClick={() => setSelectedIdx(i)}
      className="absolute border-2 cursor-pointer"
      style={{ left: `${d.x1}%`, top: `${d.y1}%`,
               width: `${d.width}%`, height: `${d.height}%`,
               borderColor: DEFECT_COLORS[d.class_id],
               boxShadow: isSelected ? `0 0 12px ${DEFECT_COLORS[d.class_id]}` : 'none' }}>
      <span className="absolute -top-5 left-0 text-[8px] px-1 rounded font-bold text-white"
            style={{ backgroundColor: DEFECT_COLORS[d.class_id] }}>
        {d.className} {Math.round(d.confidence * 100)}%
      </span>
    </div>
  ))}
</div>
```

### 6.3 缺陷颜色映射

| class_id | 名称 | 中文 | 颜色 | 色值 |
|----------|------|------|------|------|
| 0 | missing_hole | 漏孔 | 红 | `#ff453a` |
| 1 | mouse_bite | 鼠咬 | 橙 | `#ff9f0a` |
| 2 | open_circuit | 开路 | 紫 | `#bf5af2` |
| 3 | short | 短路 | 蓝 | `#0a84ff` |
| 4 | spur | 毛刺 | 绿 | `#30d158` |
| 5 | spurious_copper | 残铜 | 黄 | `#ffd60a` |

---

## 7. 交互流程

### 7.1 RV1126B 实时检测

```
点击「RV1126B 实时检测」Tab
  → 进入实时检测界面
  → 输入 RTSP 板端视频流地址（默认 rtsp://192.168.1.100:554/live/pcb）
  → 点击「连接板端」
  → WS 接收 targets_stream → 实时渲染 6 色缺陷框 + 板端状态（FPS/NPU/温度）
  → 缺陷详情位于画面下方（卡片网格布局）
  → 底部实时缺陷列表 + 分类统计
  → 可选：Gemini AI 分析检测画面
```

### 7.2 图片检测

```
拖拽/选择 PCB 图片
  → 保存 originalFile + 显示预览 (base64)
  → 点击「开始检测」
  → POST /api/detect/image (multipart: file + confidence + iou)
  → 后端返回 { detections: [{class_id, className, confidence, x1,y1,x2,y2}], debug: {...} }
  → 渲染 6 色缺陷框 + 底部统计栏（各类型数量）
  → 点击缺陷框 → 右侧面板显示详情（类别/置信度/位置）
```

### 7.2 参数调节

各 Tab 的滑块（置信度/IOU）为本地状态，检测时随请求发送给后端。全局配置通过 WS `CONF_UPDATE` 同步。

---

## 8. WebSocket 通信

### 8.1 连接

`wsClient.ts` 单例，自动连接 `{protocol}//{host}/ws`，Vite 代理到 `localhost:5000`。断线 3 秒自动重连。

### 8.2 消息格式

| 方向 | 消息 |
|------|------|
| 后端→前端 | `{ type: "targets_stream", data: [...] }` |
| 后端→前端 | `{ type: "telemetry_metrics", data: {...} }` |
| 后端→前端 | `{ type: "status_change", data: { connected: true } }` |
| 后端→前端 | `{ type: "log_broadcast", data: { message: "..." } }` |
| 前端→后端 | `{ method: "CONF_UPDATE", channel: "control_channel", data: {...} }` |

### 8.3 数据适配

`dataAdapter.ts` 负责后端 0-100% 相对坐标 → 前端像素坐标（1100×620 基准）的转换，以及字段名映射（`npuUtilization`→`npu` 等）。

---

## 9. 改造状态（已完成 ✅）

前端已从原交通检测系统完全改造为 PCB 缺陷检测系统：

| # | 改造内容 | 状态 |
|---|---------|------|
| 1 | `types.ts` 移除交通字段，新增 `defectType`/`confidence`，精简 `SystemConfig` | ✅ |
| 2 | `Sidebar.tsx` 菜单改为 PCB 检测场景（新增 RV1126B 实时检测） | ✅ |
| 3 | `ImageRecTab.tsx` 缺陷框用 6 色映射，详情面板显示缺陷名称和置信度 | ✅ |
| 4 | `VideoRecTab.tsx` 适配 PCB 场景，移除轨迹线，新增 Gemini AI | ✅ |
| 5 | 删除 `CameraTab.tsx`（原 RTSP 交通摄像头） | ✅ |
| — | **新增** `LiveDetectTab.tsx` — RV1126B 板端实时 PCB 缺陷检测（含缺陷详情画面下方 + Gemini AI） | ✅ |
| — | `ConfigTab.tsx` 重构为五大板块：模型/存储/板端参数/告警/系统信息 | ✅ |
| — | `HistoryTab.tsx` 简化为缺陷检测记录 | ✅ |
| — | `LoginScreen.tsx` 从 RV1126B 交通平台改为 PCB 缺陷检测系统 | ✅ |
| — | `start.py` 一键启动前后端 | ✅ |
